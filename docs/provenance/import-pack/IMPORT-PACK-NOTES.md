# Scope of this transport projection

This smaller package resolves the large-chat-attachment problem. It intentionally separates Git-reviewable source from the complete preserved reconstruction environment.

The original root README/AGENTS/CLAUDE are replaced here by these transport-aware instructions. Original handoff and application documentation retains its exact bytes; historical instructions to verify every full-backup member apply only to the complete reference. Whole-backup manifests and verifier/audit runners are omitted to avoid presenting them as a usable gate in a partial clone. Original file hashes are preserved in IMPORT-PROVENANCE.json.

Current source/test/tool files remain at their original paths. The APK extraction helper referenced by the 604 builder is retained. Executable modes are recorded and retained. Original source may refer to omitted native/runtime inputs; dependency installation or a new native project must not be inferred from this import.

Selected small compiled Android manifest references and synthetic recovery-envelope fixtures are retained as evidence/test inputs. They are not editable application source or patient vaults. Retained Java decompilations and generated function disassembly are explicitly reconstruction evidence, not original authored native code.

The recovered `Solaris-Android-Reconstruction/src/recovered-601/worker/qvac-worker.bundle` is omitted as a generated dependency bundle. Its retained entry file contains old absolute dependency paths and is not complete, directly runnable worker source.

No public-repository redistribution clearance is implied. Retained third-party/recovered material remains subject to its existing notices and further audit. This package intentionally excludes bundled third-party node_modules; exact copies and notices remain in the complete backup. Private-data exclusion and a bounded review are not a comprehensive secret/security audit.

For the easiest phone route, upload this small transport archive once to a new private import branch through GitHub's browser file picker. Claude Code can then clone the branch, inspect and extract the archive into a fresh temporary directory, verify it, and import the files. A ZIP/TAR upload does not automatically unpack into source files. Remove the transport archive from the final working tree after successful import; its earlier Git object remains in history. This explicit one-time exception does not permit committing the four full backups, APKs, models or tool archives. If avoiding even a small transport object in Git history matters, use a private release asset and an authenticated Codespace instead.

The source import prompt narrows the previous setup brief's full-reference prerequisite: source import, documentation and applicable source-only checks can proceed now. Full 604 reproduction remains blocked until its exact large inputs are separately restored. No new Android behavior is authorized by either setup route.
