# Next bounded execution: public source, trustworthy chat, native recovery

18 September 2026 · Apply to the existing Solaris Android workspace

This plan reconciles the [audit dispositions](../reports/AUDIT-DISPOSITION-2026-09-18.md) with reported local work. It authorizes the already requested bounded source/CI/chat/native continuation and reviewed public pushes. It does not authorize every later roadmap phase, a merge, signing/installing an APK, deployment, key changes, wallets or fund movement.

## 1. Resume the actual work; do not restart it

Claude last reported local `9183802`, seven commits beyond `edd43bd7404730480f3c5bd94720a3aef068fdb9`, on the import task. Resolve current full HEAD, actual branch/upstream, merge base, PR #1 state and dirty/untracked work before edits. A shorter old hash is not proof of current state. Do not reset, cherry-pick over newer work, rebuild from the archive-only branch or create a replacement repository. If the original workspace is unavailable, identify the exact missing commit bundle; preserve and continue whatever reviewable work is available without inventing those changes.

Create and verify a local checkpoint bundle/patch and separately account for unfinished files; keep exports out of public Git unless their complete contents are cleared. A private backup is useful for restricted originals, but not a prerequisite for a reviewed safe source push. Public Git can durably preserve cleared source. Preserve frozen evidence and record new candidate changes separately.

Public `TheMajicCode/Solaris-andriod` is the authorized destination. Update stale operative privacy/backup clauses narrowly and close the old visibility/hook conflict once. Keep health data, credentials and signing/wallet secrets private. Inspect the outgoing range, newly reachable history and proposed artifacts for concrete restricted material; exclude affected new additions and continue unaffected authored work. No silent history rewrite or key rotation.

## 2. Close A604-01 on its actual candidate

One writer owns repository governance/checker/CI/classification paths. An independent reviewer receives the actual full diff, exact current commit, original requirements, candidate manifests and sanitized check logs. Prior self-review or a timed-out agent is not approval.

Required checks:

- Frozen source and transport references remain pinned; intentional candidates use original/result hashes, rationale and integration target. Reconcile file-count differences by role; do not force all scopes to an old count.
- Every maintained path is classified; missing/empty required inputs, missing dependencies, exceptions, unknown files and unregistered defects fail. Keep unavailable native/reference jobs distinct from passing source CI.
- AND-IMP-01 is only its exact path/hash exception; another broken file still fails. Preserve the five descriptor allowlist entries where appropriate.
- AND-CI-01 is tested under the actual pinned Node runtime and intended module modes, with valid ESM/CJS positives plus malformed negatives. Pinned PyYAML with hashes must be installed before its required check.
- Preserve and rerun the meaningful checker negative controls, including candidate mutation, frozen mutation and an unclassified maintained path. Prevent changes to the classification/manifest policy from laundering arbitrary source drift.
- Pin all remote Actions, keep ordinary public PR jobs read-only and free of signing secrets, and do not execute untrusted PR code in a privileged workflow. Preserve real failing exit status; do not reinstate an always-failing placeholder or count SKIPPED as PASS.

Local checks plus independent review of the exact candidate and outgoing-content inspection precede the authorized push. Push the existing branch and update draft PR #1, then run actual remote CI. Record branch-head SHA separately from synthetic merge SHA. Do not demand remote CI for an unpublished commit before it can be pushed. Address failures, review material follow-ups and rerun affected checks; do not auto-merge.

Do not block all source development waiting for a merge. Chat and native work can use the exact reviewed foundation checkpoint in separately owned dependent branches/worktrees. Record base dependencies and revalidate after base changes. If remote CI is unavailable, label it blocked and keep foundation acceptance incomplete while eligible local work continues.

## 3. Review and integrate A605-01/02 together at the trust boundary

Inspect the existing `candidate/` implementation before editing; do not create a second unused router. Write/update the short contract from measured baseline behavior and the findings matrix. If code or tests already satisfy a requirement, retain them and establish evidence rather than rewriting for style.

The supported routes should retain useful greetings/capability help, selected check-in explanation and bounded guided reflection/next-step intents. Preserve the open text box for these requests. Do not solve truthfulness by silently rejecting every request or routing every input into unrestricted inference.

The conservative default for unsupported/ambiguous requests in this bounded repair is a clear deterministic limitation/clarification and supported choices. A symptom keyword list cannot establish that every unmatched request is harmless. Risk/out-of-scope handling precedes wellness matching; no mixed clinical request becomes ordinary check-in reassurance. EN/ES escalation wording remains an engineering placeholder until qualified review before patient release. Do not advertise medical triage completeness.

Personal facts must come from explicitly selected, approved fields in current records. Validate subject/authority, source ID/type, record revision, allowed field, units/value and missing-versus-zero semantics. Render factual statements from those typed bindings; attaching an unrelated `sourceRefs` entry to arbitrary prose is insufficient. Treat source text as data, not instructions. Missing/unapproved/stale information stays unavailable.

Where module 958 remains unchanged, name the new behavior **containment**, not a parser repair. Enforce it at all actual consumers: rejected raw output cannot briefly appear in streaming UI, become persisted chat, enter a receipt or contaminate later context. Recheck authorization after relevant awaits, including receipt authentication and commit. Preserve lock/cancel/background and edited-draft behavior.

| Required regression group | What must be demonstrated |
| --- | --- |
| F04 EN/ES variants | Original text preserved; matching handles inverted punctuation, accents, whitespace and polite wrappers; whole-intent matching does not admit mixed requests |
| F05 mixed/unknown | English and Spanish mixed symptom/check-in cases, `dolor en el pecho`, `me duele el pecho`, `meds`, paraphrases and false-positive controls; unknowns cannot enter unrestricted personal/clinical prose |
| F03 adverse and positive | Original seven parser probes plus unrelated/unknown source, stale revision, unapproved field, absent value, genuine source-backed contradiction, authority changes, prototype fields/type-coercion; also harmless greetings, truthful missing-data responses and supported accurate selected facts |
| Actual integration | Admission→route→answer boundary→readable/compact UI→authorized persistence→receipt/context; same relevant matrix on frozen 604 and final candidate, with measured expected results |
| Cold and warm paths | Spy on initialization/model loading/tokenization/completion in unloaded and loaded states. Zero completion calls in a preloaded fixture does not prove no model-preparation delay |
| Race and preservation | Cancel/lock/background, source delete/change, permission/owner change, post-authentication awaits, error cleanup, idempotence and preservation of unrelated drafts |

Synthetic injected outputs, real-model evaluations and phone behavior are separate evidence. Do not claim general model quality from parser injections or a universal speed improvement from guided routes.

## 4. Restore exact host inputs and native build in parallel

Use [NATIVE-RECOVERY-INPUTS.md](../NATIVE-RECOVERY-INPUTS.md) and reconcile its full pins against the existing N1–N7 inventory. Check local available inputs before asking Majd for files. Restore only what is actually missing:

1. Exact 603 HBC and pinned `hermesc` can support applicable compilation/patch work.
2. The current full builder requires the exact 603 APK, not HBC alone. Actual-host comparisons may separately require the matching Hermes runtime/harness.
3. These inputs do not supply a recovered Gradle/native project. Native restoration needs authored/recovered/reference classification, toolchain/ABI/version locks and a clean source-backed unsigned build.

Verify acquisition provenance, archive paths, full hashes and runtime prerequisites before executing tools. Public upstream restoration is allowed for non-sensitive tools; binary backups and restricted inputs stay outside ordinary Git. No dependency upgrades to chase current Pear/WDK examples in this workstream.

Preserve package ID, signer lineage, subject identity, record IDs/revisions, models, vault/recovery formats and attachments. Keep F06's race a hypothesis until actual native execution establishes it. Inventory the 16 MiB recovery-envelope boundary, source/attachment treatment, OEM backup/transfer and upgrade compatibility. Do not solve missing source by labeling a generic scaffold as the recovered app.

Respect the measured 604 HBC/HTML patch boundaries and remaining capacity. If the answer contract cannot fit and be verified, deliver reviewed source/contracts/tests and continue native restoration; do not weaken it or claim an unused helper is an APK repair.

## 5. Later work, with dependencies kept explicit

| Workstream | Start/exit condition |
| --- | --- |
| A606-01 native durability | Enough actual native execution for lock/import/new-operation, crash/disk-full, invalidated key, attachment, second-device restore and existing-user upgrade tests |
| A606-02 pending turns | Existing native persistence contract understood; encrypted pending turn and operation ID, explicit retry after unlock/current authorization, idempotent uncertain-ack recovery; no browser plaintext or silent replay |
| A606-03 quality/performance/accessibility | Capture authorized exact-device baseline first. Separate cold/warm p50/p95 feedback/token/completion/cancel, RAM/thermal/battery and useful grounded-answer quality. Retain Sanctuary design and verify source preview, TalkBack, large text, contrast, keyboard and reduced-transparency behavior |
| REL-01 production | Exclusive signing custody and tested existing-user transition, source-to-artifact/SBOM/license/CVE disposition, supported-device acceptance, qualified clinical-copy review and independent final artifact review |

The earlier 250 ms acknowledgement / one-second warm-guided p95 / 500 ms stop-feedback values are proposed targets, not measured results or imposed model-quality tradeoffs. Continuous background generation and larger models are separate decisions. Prior 49–93 s observations do not establish current candidate performance.

## 6. Carry the ecosystem discoveries forward without expanding this patch

Retain the [product boundaries](../ECOSYSTEM-AND-PRODUCTS.md), [GPS/RGB roadmap](../GPS-RGB-CONTRACT-ROADMAP.md) and dated research. Pear/Bare mobile embedding supports a later compatibility investigation; it is not a reason to replace the inherited native stack today. WDK/Breez and RGB implementations have separate platform, trust and recovery requirements. Recheck current upstream versions before an integration spike.

GPS is Global Prosperous Split, not phone geolocation: first a policy/evidence/allocation/receipt framework, later an optional settlement adapter. Preserve one root `ValueEpisode`, separate historical profiles and the 9%-versus-10% / cascade discrepancies until ratified executable vectors resolve them. RGB does not prove care or execute the entire GPS policy. No AI-invented allocations, health data in public receipts or financial side effects inside replayable reducers.

Deliver in order: reliable local vault/chat → consented synthetic patient/clinic exchange → GPS shadow evidence/receipts → separately scoped wallet and RGB labs → capped independently authorized funded pilot. Clinic OS is practitioner-controlled software; Clinic in a Box is later supported packaging. Research and contracts can progress alongside native recovery; they do not close its gates.

## Required return from Claude

Return the actual full base/head SHAs, branch/PR links and checkpoint preservation status; changed paths and finding dispositions; independent reviewer/result and exact reviewed revision; commands/exit codes and local versus remote CI SHAs; restored input hashes and consumer commands; source-only versus actual host/native milestones; and exact blockers/next bounded task. Report any unsigned output and its hash, and say explicitly whether an APK exists. Signing, installation, merge and deployment remain unauthorized. Do not repeat obsolete visibility refusals or count documentation review as application approval.
