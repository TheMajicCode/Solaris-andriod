# A1 recovered lifecycle follow-up

The owner's current “proceed with best next steps” continues the authorized reversible A1 repair work. This is a source-independent integration candidate based on the exact SDK and compiled service evidence recovered from V6 code 600. It does not replace or claim recovery of the missing native repository, governance, build environment or signer.

## Baseline and scope

- Preserve uploaded V6 APK SHA-256 `4ac6a72c864118ec548ee5c05738c814b6e519f82b01a7d358d24952b900c518`, original decoded UI and all previous saved checkpoints.
- The existing recovered-UI amendment and latest handoff remain applicable. Surviving D4/D3 source directories were checked and contain no files. No new native source or signing evidence was found. Repeating the full recovery search is not this task.
- Root writer owns `src/`, `test/`, `evidence/`, root contract, packaging and handoff in this new isolated directory. The adapter will use only verified state/resume contracts and retain existing request/session cancellation and model storage boundaries. No APK dependency or framework change.
- API investigator owns `review/API-EVIDENCE.md`. Independently confirm actual SDK response shapes and a meaningful host-test route. A separate reviewer will review the implemented helper and complete diff.
- UI writer owns `ui-followup/` only: two bounded EN/ES Health Connect wording corrections. Permission does not establish returned readings; avoid implying a separate wearable/tracker is always required. All existing behavior and earlier UI repairs are preserved in a new copy.

## Acceptance

The adapter must admit no operation before confirmed active SDK state; preserve current-operation checks across every awaited boundary and before releasing a result; never retry a dispatched load/inference; retain uncertain in-flight ownership after timeout/cancellation; expose no record contents/keys in diagnostics; add no model download/storage or phone modification path. Existing service lifecycle queue/identity-loop protection must remain an integration prerequisite, not be replaced by this helper.

Test the exact recovered SDK lifecycle module and handlers with explicitly synthetic resource and import stubs, if possible. Verify startup while suspended, an opposite transition in progress, failed resume, timeout with late settlement, invalidated request and concurrent admission. Distinguish host SDK-module evidence from a complete SDK/native worker or phone run. Record any unsupported route precisely. Do not claim the phone failure is conclusively reproduced.

Deliver source, hashes, executed checks, independent review and integration notes; add this work to a durable consolidated recovery package. The original native source ZIP, full lockfile and signing environment remain build blockers. No D4/V5-B reconstruction, original-source claim, commit/push/merge, deployment, phone install/reset, signer/key change, funds, network sharing or contacts.
