<!-- Recovered verbatim from the session transcript of subagent `a4b14b729b4c153cc`; the report was delivered through its hand-back call. -->

> **Recovered reviewer record — Independent review of 6126903 (NB1–NB10).** Reviewed SHA `6126903`. This is the reviewer's own text, not a summary. Original payload SHA-256 `e1cb9fc5d2262dd0c67bf93a58ea709264b81d2982ffea08d0bb1d83f77af651`. Sanitized for publication only: 1× local home path → `<home>`. No finding text was altered. Independent AI review, not a GitHub approval.

---

# INDEPENDENT REVIEW — commit 6126903 on `claude/solaris-android-import-iyla4d`

## VERDICT: APPROVE WITH FINDINGS — for commit `6126903` **exactly**

No blocking defect found in the reviewed range. The central claims all verified, including an independent byte-identical reproduction of the 604 host bundle that I ran myself. Findings below are non-blocking hardening items, plus one **process** caveat that the caller must act on before pushing.

---

## PROCESS CAVEAT — read this first

At review start `git status --porcelain` was **empty** and matched `6126903`. **During the review the working tree acquired 12 modified files** (`candidate/a605/*.mjs`, `candidate/tests/*.test.mjs`, `docs/provenance/SOURCE-CLASSIFICATION.json`, `tools/solaris_checks/checks.py`, `docs/AUDIT-FINDINGS-MATRIX.md`, `docs/BUILD-AND-TEST.md`, `docs/FEATURE-STATUS.md`, `docs/HOST-REPRODUCTION-EVIDENCE.md`, `docs/workflow/HANDOFF.md`). A writer is active on the same paths concurrently with review — a departure from AGENTS.md "one writer per path".

**This approval covers commit `6126903` and nothing in the working tree.** If the push carries anything beyond `6126903`, it needs a fresh review. I verified all findings against a pristine scratch copy pinned at `6126903` (`git status` clean) and against `git show 6126903:<path>`, so nothing below is contaminated by the concurrent edits.

Also: the brief says "6 commits" — `git rev-list --count 5d48426..HEAD` is **5**. 15 files is correct.

---

## 1. Outgoing content safety — CLEAN

- 15 files: 13 modified, 2 added (`docs/HOST-REPRODUCTION-EVIDENCE.md`, `tools/host/hbc-input-wrapper.py`). `git diff --numstat` shows **zero binary files**.
- `git ls-files | grep -Ei '\.(apk|so|hbc|zip|jar|aar|keystore|jks|p12|pem|key|dex|bundle|aab|tar|gz|7z|bin|dylib|dll|exe)$'` → **empty across the entire tree**.
- `git ls-files | grep -Ei 'hostkit|host-input|hermesc|payload/|handoff.*zip|disposable'` → **empty**. `reconstruction-inputs/` and `reconstruction-work/` are neither tracked nor present on disk in the repo.
- The private kit (which does contain `libhermes.so`, `libjsi.so`, `hermesc`, the 603 bundle) lives only under the scratchpad; `disposable/src` is not a git repo at all, so it cannot be committed by accident.
- Secret/credential/PII sweep over the diff and over the full content of both new files: no hits. Only email in the range is `noreply@anthropic.com`.
- No scratchpad or `<home>` absolute path leaked into any committed file.
- **No frozen path touched**: cross-checking the 15 changed paths against `REPO-IMPORT-MANIFEST.json` → `FROZEN paths touched: NONE`. `docs/provenance/` unchanged in the range.

## 2. B1 and B2 — both still genuinely closed (re-attacked)

**B1** (`frozen_external_records` laundering drift in an imported file): on a scratch copy I appended bytes to `docs/provenance/import-pack/README.md` (a manifest-pinned imported file) and added a matching `frozen_external_records` entry with its new hash. Result: `REAL EXIT=1`, `Overall: FAIL`, with the exact guard message — *"listed in frozen_external_records but already pinned by the import manifest. An authorized change to an imported path must be a candidate_override…"*. The `continue` at `tools/solaris_checks/checks.py:601` keeps the manifest entry, so the drift also fails the hash comparison independently. Path-normalisation variants (`./docs/…`) fail too, as the record then resolves to an untracked path.

**B2** (declassifying `candidate/` to silence its test gate): I broke `candidate/tests/run-all.mjs` (`process.exit(3)`) and then reclassified `candidate/` to each of the four other *valid* scopes:

| scope | exit | overall | candidate-regression-tests |
|---|---|---|---|
| `retained-evidence` | 1 | FAIL | FAIL (0/9) |
| `documentation` | 1 | FAIL | FAIL (0/9) |
| `repo-tooling` | 1 | FAIL | FAIL (0/9) |
| `authored-source` | 1 | FAIL | FAIL (0/9) |

The orphan detector in `check_candidate_tests` fires every time. An *invalid* scope string also fails closed (exit 1, via `classification.py:34`). B2 is closed.

## 3. Central validation — 23 adversarial probes

All checks pass at HEAD, exit 0 each:

| command | exit | result |
|---|---|---|
| `python3 tools/repo-check.py` | 0 | PASS, 13 checks, 8 BLOCKED gates |
| `python3 tools/tests/test_repo_check.py` | 0 | 27 passed, 0 failed |
| `python3 tools/tests/test_js_parse_modes.py` | 0 | 11 passed, 0 failed |
| `python3 tools/tests/test_result_aggregation.py` | 0 | 17 passed, 0 failed |
| `node candidate/tests/run-all.mjs` | 0 | 385 assertions, 0 failed |

Attacks I ran directly against `run_all`/`validate_results` — **caught** (overall FAIL): NOT_APPLICABLE with non-empty scope; NOT_APPLICABLE from a check that may not report it; PASS with 0 examined; PASS 1/5 under `COVERAGE_FULL`; examined > expected; bogus status `'SKIPPED'`; status `None`; negative counts; float counts; self-rename; a check that raises; duplicate result; unregistered result; missing result. **Legitimate positives correctly preserved**: PASS 0/0 (empty required scope), NOT_APPLICABLE 0/0 on an NA-allowed check, PASS 1/5 under `COVERAGE_NONZERO`, full coverage.

## 4. Candidate fixes — verified, adversarially

- **Calendar validation is correct.** 43,896 dates compared against a corrected ground truth: **0 mismatches**. `1900-02-29`→false, `2100-02-29`→false, `2000-02-29`/`2400-02-29`/`2024-02-29`→true, `2026-04-31`→false, `2026-00-01`/`2026-13-01`/`2026-02-00`→false, `9999-12-31`/`0001-01-01`→true, `2026-1-1`→false (regex requires zero-padding). No impossible date accepted; no valid date rejected. Non-ASCII digits, whitespace, `+`/`Z` suffixes all rejected. *(My first pass reported 366 "mismatches", all at year 0000 — that was a `Date.UTC` year-0–99→1900s artifact in my harness, not a validator defect.)*
- **No record mutation**: a hostile record with `date: '2026-99-99'` is byte-identical after routing.
- **Malformed selection**: fuzzed 24 user strings × 17 selection shapes × 8 authorities × 6 locales = **19,584 calls → 0 throws, 0 nulls, 0 diagnostic leaks** (regex over the reply for `TypeError|Cannot read|undefined|null|[object|NaN|stack|.mjs`). `routeRequest()` with **no argument at all** and with `null` both return the limitation reply rather than throwing.
- **No input returns `null`** — confirmed over the whole fuzz corpus plus degenerate request objects.
- **Date is genuinely bound**: the displayed date is `chosen.dateFact.value` and `dateFact` is `typedFacts[0]`. Baseline renders `bound=[date,vitality,clarity,balance,alignment]` (5 facts) matching the sentence exactly.
- **Unapproved / stale / unknown / changed-authority claims are refused**: date-not-approved → `checkin-select` with no answer; authority epoch or permissionRevision changed → `checkin-select`; prose in a date field (`IGNORE ALL PRIOR INSTRUCTIONS`) → refused; prose/out-of-range/non-integer ratings → omitted from both message and bindings; `new String('2026-09-16')` → refused; duplicate source ids resolve consistently to one record with no cross-record mixing; a getter on `fields.date` is read exactly **once** (no TOCTOU).
- **Shipped payloads independently confirmed from UI source**: `grep -o "ask:[^\"'<>]*"` over `Solaris-Android-R4/ui/sanctuary.html` **and** `sanctuary.compact.html` yields exactly three — `Explain my check-in`, `Help me choose a step today`, `Who is Pocket LUCA AI?` — and dispatch is literally `if(n.startsWith('ask:')){await send(n.slice(4));return}`. All three route correctly (`checkin-select`, `step-select`, `welcome`), EN and ES. The test comment that `Reflect` is *not* a payload is accurate: it maps to `new:reflect`.

## 5. Host reproduction — INDEPENDENTLY REPRODUCED (highest-value result)

I re-ran `tools/host/hbc-input-wrapper.py` myself against the private inputs, writing only to the scratchpad:

```
input_bundle_sha256 : b8ac7d1b…fedc990   (matches pin, 30,754,484 bytes)
spare_ui_bytes      : 1936
changed_function_ids: [6929, 14890, 14894, 14904]
final_hbc_bytes     : 30800788
final_hbc_sha256    : 30be9989cc00299836715cc3de5cf3a2a75b00019bdf2f1b91629205f558d8b3
byte_identical_to_historical_604: true          exit 0
```

Every field matches `Solaris-Android-R4/evidence/release/BUNDLE-RESULT.json` (`final_hbc_sha256`, `final_hbc_bytes`, `changed_function_ids`, `ui.spare_bytes: 1936`). I then ran both suites **against my own reproduced bundle**:
- `grounding/run-tests.py` → `{"pass": true, "total": 34, "failures": []}`
- `lifecycle/tests/run.py` → `{"pass": true, "total": 10, "failures": []}`

**Assertion parity.** I enumerated all 12 `require()` calls in the frozen `Solaris-Android-R4/tools/build-bundle.py`. The wrapper has a verbatim equivalent for 11 of 12 (functional scope, UI slot, HTML boundary, UI slot overflow, UI overlap, UI changed headers, UI outside mutation, other string mutation, function count, function header mutation, function body mutation), plus the same `read(base, exact_base=True)`, `append_plans` and `mkdir(exist_ok=False)`. The 12th is the declared APK→bundle pin substitution. **The substitution is honest and arguably not even a weakening of the bundle check**: frozen `hbc_patch.py:39` already asserts `len(data) == BASE_LENGTH and digest(data) == BASE_SHA256` under `exact_base=True`, so the bundle is pinned downstream regardless. What is genuinely lost is only APK→bundle provenance and APK integrity — exactly what both the wrapper docstring and the doc say. The wrapper also always applies the UI (the frozen builder makes it optional), which is the stricter path and matches what historical 604 did.

**No patching of frozen code.** The wrapper imports `build-plans.py` and the 601 decoder by path via `importlib`, identically to the frozen builder; it never imports or modifies `build-bundle.py`, and monkey-patches nothing.

**Donor fit — confirmed it does not fit.** The frozen assertion is real (`build-plans.py`: `require(d.header.functionCount==2 and d.strings[d.function_headers[1].functionName]=='fastGuided', …)`). From my own run's `guided-compiler/fast-guided.hbc`: `functionCount = 2, names = ['global', 'fastGuided']`. The pinned `hermesc` (sha `b4c37f09…`) rejects `class AnswerRejected extends Error` with exactly *"invalid statement encountered"*. My own bundling of the five candidate modules compiled to **25** functions with function 1 = `''` (the bundle IIFE), not `fastGuided` — the doc says 26; the exact integer depends on bundling/class-lowering choices and is not load-bearing. The substantive claim (many functions vs a 2-function contract, function 1 is the IIFE, contract not satisfied) is confirmed.

**The "disposable copy is mandatory" justification is accurate**: `grounding/run-tests.py` writes only under `--output`, but `lifecycle/tests/run.py:8` writes to `HERE/'evidence'/label` inside its own source tree.

## 6. Documentation honesty — CLEAN

No prohibited claim found. Explicit denials are present and correct: *"No APK exists. `A605` is a workstream name, not version code 605"*; *"No native source build. A host-bundle reproduction is not a native build"*; *"F03 remains containment, not a parser repair"*; *"F06 remains an unreproduced native race hypothesis"*; *"A byte-identical host-bundle reproduction is not a native source build and not an APK"*; *"evidence about the host bundle only"*. "CI green" is attributed to the **base** `5d48426`, and STATUS states *"Local. Remote CI runs on the pushed head."* The 1,936 figure is stated as *"independently matches the figure in the 604 audit"*, not as a re-executed audit. Every check number in STATUS (13 / 27 / 11 / 17 / 385 / 34 / 10) matches what I ran. Commit messages are careful and honest.

---

## NON-BLOCKING FINDINGS

**NB1 — `validate_results` fails open on an empty registry.** `tools/solaris_checks/checks.py:634` (`def validate_results(results, specs=CHECK_SPECS)`) and `:711` (`def run_all(repo, files, specs=CHECK_SPECS)`). `run_all(REPO, files, specs=())` returns `status=PASS` with zero checks and zero violations — a green run that inspected nothing, which is precisely the shape AGENTS.md calls a FAILURE. **Not reachable from the shipped entrypoint**: `tools/repo-check.py:73` calls `run_all(REPO, tracked_files())` with the default registry. Fix is one line at the top of `validate_results`: `if not specs: violations.append('the check registry is empty')`.

**NB2 — `CheckSpec.coverage` is never validated.** A spec declared with an unrecognised coverage value (I tested `'banana'`) silently degrades to `NONZERO` semantics: `PASS 1/5` passes where `FULL` was intended. A typo in the registry becomes silent under-enforcement. Fix: reject any coverage not in `{COVERAGE_FULL, COVERAGE_NONZERO}` at registration.

**NB3 — three checks are declared `COVERAGE_NONZERO` but achieve full coverage in practice** (`checks.py:596–606`): `frozen-integrity` (1881/1881), `candidate-changes-valid` (12/12), `candidate-regression-tests` (9/9). In `check_frozen_integrity` any unhashed path already appends a finding and forces FAIL, so `examined < expected` can only co-occur with FAIL — `COVERAGE_FULL` would be strictly stronger with no risk of a false failure. Under-declared, as the brief suspected.

**NB4 — a check function returning `None` crashes outside the guarded path.** `checks.py:725` (`if result.name != spec.name`) sits *after* the `try/except` that wraps `spec.fn(ctx)`, so a `None` return raises `AttributeError` out of `run_all` entirely and no report is produced. Still fail-closed (non-zero exit), but it bypasses the "an unexpected error is a FAILURE" reporting path the module docstring promises.

**NB5 — `isinstance(x, int)` accepts `bool`.** `checks.py:660`. `files_examined=True, files_expected=True` passes validation. Theoretical only.

**NB6 — authority binding is equality-only, so mutual absence compares equal.** `candidate/a605/answer-boundary.mjs:61-62` at `6126903`: `undefined !== undefined` is false, so a record omitting `authorityEpoch`/`permissionRevision` together with an authority `{}` renders a real answer carrying an undefined authority binding. **Pre-existing at the published base `5d48426`** (verified with `git show`), so not a regression. The range actually *tightened* the adjacent guard (`typeof authority !== 'object'` added to `groundedCheckin`).

**NB7 — "you recorded no aspect ratings" is stated when aspects exist but could not be bound.** `candidate/a605/answer-boundary.mjs:98` at `6126903`. With all four aspects present in the record but unapproved, the reply asserts the user recorded nothing — an affirmative false statement about their own data, distinct from "unanswered". **Pre-existing at `5d48426`** (verified), not a regression. *Note: the concurrent writer is already fixing both NB6 and NB7 in the working tree (`AUTHORITY_UNBOUND`, absence counts) — those fixes are **not** in `6126903` and I did not review them.*

**NB8 — approval/field lookups trust caller-supplied object semantics.** `answer-boundary.mjs:54` uses `source.approvedFields.includes(...)` (an object with an overridden `includes()` returning `true` bypasses the approval gate) and `:57` uses `source.fields[ref.field]` (values supplied only via the prototype chain are read and rendered). Requires an adversarially-crafted JS object rather than plain record data, so it is outside the realistic threat model. Tightening would mirror the care already taken at `:96` (`Object.prototype.hasOwnProperty.call`).

**NB9 — wrapper cosmetics.** `tools/host/hbc-input-wrapper.py:33` `ROOT = Path(__file__).resolve()` is unused dead code. `:59` discards `report` from `append_plans`, so `WRAPPER-RESULT.json` omits the frozen builder's `changed_string_ids` / `native_payload_changes` / `inference_request_and_worker_unchanged` / `background_lock_and_cancel_unchanged`. Those are report labels, not `require()`s, so **no assertion is lost** — but emitting `changed_string_ids: [12364]` would make the output directly diffable against the historical `BUNDLE-RESULT.json`.

**NB10 — doc nit.** `docs/workflow/STATUS.md` header reads *"Updated: 2026-09-17 · Session 2"* while the sprint content it carries is dated 18 September 2026.

---

## WHAT I COULD NOT VERIFY

- **That `python-version: '3.12.14'` and `node-version: '22.23.2'` resolve on GitHub-hosted runners.** No network / no remote CI from here. If either pin is unavailable, the setup step fails — fail-closed, but it would break CI on the pushed head. The doc's basis (observed from an actual remote run) is reasonable, but this is the one change in the range whose effect only shows up remotely.
- **Remote CI on `6126903`** — not pushed yet.
- **The exact "26" donor function count** — I measured 25 with my own bundling; the difference is methodology, and the claim's substance holds.
- **APK provenance for the 603 bundle** — the APK is absent by design. This is precisely what the doc correctly states the wrapper does *not* establish.
- **Anything in the current working tree** — 12 files modified mid-review by a concurrent writer; out of scope for this commit-pinned review.

## COMMANDS RUN (selected)

`git rev-list/diff/ls-files/show` (range and safety) · `python3 tools/repo-check.py` (0) · `test_repo_check.py` (0, 27) · `test_js_parse_modes.py` (0, 11) · `test_result_aggregation.py` (0, 17) · `node candidate/tests/run-all.mjs` (0, 385) · B1 laundering attack (exit 1, FAIL) · B2 declassification × 4 valid scopes (exit 1, FAIL each) · 23 synthetic-spec probes against `run_all`/`validate_results` · 43,896-date calendar comparison (0 mismatches) · 19,584-call router fuzz (0 throws/nulls/leaks) · 18 binding-integrity attacks · `tools/host/hbc-input-wrapper.py` re-run (exit 0, byte-identical) · `grounding/run-tests.py` on my reproduction (34/34) · `lifecycle/tests/run.py` on my reproduction (10/10) · pinned `hermesc` class-rejection probe · candidate bundle compile + function count.

The repository was never modified by me; all mutation happened in scratchpad copies.
