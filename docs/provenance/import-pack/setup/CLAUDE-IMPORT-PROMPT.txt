Set up the Solaris Android repository from the small source import package already uploaded to this private repository. Work in Claude Code's repository environment, not ordinary chat attachments.

Read the repository's existing instructions first. Verify that this is the intended private Solaris Android repo; do not touch the separate Solaris web repo. The bootstrap input branch may already contain the transport files. Continue on an allowed task branch from that input commit and open an unmerged PR to the verified default branch. Do not force-push or merge automatically.

Locate Solaris-604-GitHub-Source-Import.tar.xz and its adjacent SHA256SUMS file. Verify the archive checksum. Before extracting, inspect the archive member list and reject absolute/parent-traversal paths, symlinks, hard links, device files or unexpected roots. Extract into a new temporary directory outside the Git worktree. Python 3.12+ tarfile with filter='data' can be used after those checks; do not execute archived scripts automatically.

Read the extracted README.md, AGENTS.md and IMPORT-PACK-NOTES.md. Inspect verify-import.py, then run it in that unchanged extracted directory. It checks only this frozen source projection, not the full backup or APK. Review IMPORT-PROVENANCE.json. The original four large 604 ZIPs are not required to start this source import and documentation task. Keep full-reference reproduction and any tests needing missing tools/APKs/model/native dependencies explicitly blocked until exact inputs are restored. Never fabricate missing source or a native Gradle build.

Copy the reviewed source/docs into the repo preserving their relative paths and executable modes. Reconcile root README/governance deliberately; preserve the frozen extracted copy for provenance. Read the setup/product brief and README draft in setup/ and implement the repository documentation, accurate feature status, scoped source checks, task workflow and bounded audit roadmap. Current 604 behavior and planned P2P, wallet, GPS and sovereignty features must remain clearly distinguished. Do not implement those planned features in this task.

For this bounded bootstrap, I authorize source import, documentation/CI setup, targeted checks, independent review, a commit/push on the task branch and an unmerged PR. This explicitly permits using the one small reviewed source archive as a temporary Git transport. Remove it from the final working tree after verified extraction; acknowledge that the transport object remains in earlier Git history. Do not rewrite history to hide it. Do not commit the four full backups, models, APKs, private phone evidence or credentials. Review the staged diff/file list before pushing; obtain independent review of the exact candidate tree.

Preserve application source bytes in this import, package/signing identity, vault/recovery formats, record IDs, valid models and consent/authority boundaries. No app install, data reset, key change, new APK signing, payment, public visibility, deployment, collaborator invites or web repo changes. No bulk lint fixes or dependency upgrades.

Report repo/branch/commit/PR links, preserved source coverage, excluded inputs, exact checks run, independent review, blocked build gates and the smallest next audit task. Proceed with authorized useful work without requiring the four large archives merely to begin.
